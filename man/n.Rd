\name{DescTools Aliases}
\alias{N}

%- Also NEED an '\alias' for EACH other topic documented here.
\title{Some Aliases Set for Convenience
%%  ~~function to do ... ~~
}
\description{Some aliases are defined either for having shorter names or for following the Google naming convention.
%%  ~~ A concise (1-5 lines) description of what the function does. ~~
}
\usage{
N()
}
%- maybe also 'usage' for other objects documented here.
\details{\code{N()} is the same as \code{\link{as.numeric}()}.\cr
\code{D()} is the same as \code{\link{as.Date}()}
%%  ~~ If necessary, more details than the description above ~~
}
\author{Andri Signorell <andri@signorell.net>
%%  ~~who you are~~
}
\examples{
head(N(d.pizza$driver))
}
